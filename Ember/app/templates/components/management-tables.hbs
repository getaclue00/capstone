<table id="has-datatable" class="management-table" width="100%">
  <thead>
    <tr>
      {{#each headers as |header|}}
        <th><h1>{{header}}</h1></th>
      {{/each}}
      {{#if employeeCollection}}
        <th><h1>User Account</h1></th>
      {{/if}}
      <th></th>
    </tr>
  </thead>
  <tbody class="management-table-body">
    {{#each model as |m|}}
      <tr class="management-table-row">
        {{#each-in attributes as |key value|}}
        <td class="management-column" data-th="{{value}}">
          {{get m key}}
        </td>
        {{/each-in}}
        {{#if employeeCollection}}
          <td>
            <div class='management-operation'>
          {{#if m.user}}
            {{#link-to 'users.show' m.user.id class="management-icon"}}
              <span class="glyphicon glyphicon-user" style="color:#d30b07;"></span>
            {{/link-to}}
          {{else}}
            {{#link-to 'users.new' m.id class="management-icon"}}
              <span class="glyphicon glyphicon-plus" style="color:#d30b07;"></span>
            {{/link-to}}
          {{/if}}
        </div>
          </td>
        {{/if}}
        <td class='management-buttons'>
        {{#each-in operations as |key value|}}
         <div class='management-operation'>
          {{#link-to key  m.id class="management-icon"}}
            <span class="glyphicon {{value}}" style="color:#d30b07;"></span>
          {{/link-to}}
         </div>
        {{/each-in}}
        </td>
      </tr>
      <br />
    {{/each}}
  </tbody>
</table>
<br />