<div class="modal fade" id="appointment-time-selection" tabindex="-1" role="dialog" aria-labelledby="appointment-time-selection-show" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h6 class="modal-title" id="appointment-time-selection-show">Time Selection for: {{selectedDate}}</h6>
      </div>
      <div class="modal-body">
        <div class="container-fluid">
          <div class="form-group">
            <label for="staff" class="col-xs-12 col-form-label font-weight-bold" style="text-transform: uppercase;">Please Choice a Staff Member:</label>
            <div class="col-xs-12">
              {{#power-select
                options=listOfEmployees
                selected=aSelectedEmployee
                renderInPlace=true
                searchField="fullName"
                placeholder="Select a staff member"
                onchange=(action "selectEmployee")
                as |employee|
              }}
               {{employee.fullName}}
              {{/power-select}}
            </div>
          </div>
          {{#if wasEmployeeSelected}}
            <label for="appointment-time" class="col-xs-12 col-form-label font-weight-bold" style="text-transform: uppercase;">Available Times:</label>
            <div class="available-time-choices" style="text-align: center">
              {{#each availableTimes as |t|}}
                <button type="button" class="btn btn-outline-info col-3 time-button">{{t}}</button>
              {{/each}}
          </div>
          {{/if}}
        </div>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-danger" {{action 'toggleSelectTime'}}>Cancel</button>
      </div>
    </div>
  </div>
</div>
