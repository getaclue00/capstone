language: ruby
rvm:
  - 2.3.1
before_install:
  - cd Rails
before_script:
  - bundle exec rake db:test:prepare
script:
  - bundle exec rspec
services:
  - postgresql
# safelist
branches:
  only:
  - master
